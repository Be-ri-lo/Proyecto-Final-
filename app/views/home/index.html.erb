<div class="content">
  <!--Llamado a registrarse-->
  <div class="showcase custom-shadow mb-5">
    <div class="showcase-text m-5">
      <h1 class="showcase-text mt-5 pt-5">¿Aún no eres parte?</h1>
      <h3 class="showcase-text mt-5 pt-5">¡Atrévete a participar!</h3>
      <%= link_to 'Únete <i class="fas fa-chevron-right custom-link"></i>'.html_safe, new_user_registration_path, class: "showcase-text"  %>
    </div>
  </div>

  <!--Cómo crear un entrenamiento-->
  <section class="cards-banner-one text-center title1 p-5 m-0">
    <h2 class="pb-5 m-0 title1">Pasos para crear un entrenamiento</h2>
    <div id="step" class="news-cards row">
      <div class="col-12 col-md-6 icon1">
        <a href="">
          <i class="far fa-address-card mb-5 mt-5"></i>
          <%# <h3>Primer paso</h3> %>
          <h4 class="custom-text"> Crea tu perfil de Usuario</h4>  
        </a>
      </div>

      <div class="col-12 col-md-6 icon2">
        <a href="">
          <i class="fas fa-dumbbell mb-5 mt-5"></i>
          <%# <h3>Segundo paso</h3> %>
          <h4 class="custom-text"> Crea un entrenamiento
          </h4>
        </a>
      </div>
      <div class="col-12 col-md-6 icon3">
        <a href="">
          <i class="far fa-thumbs-up mb-5 mt-5"></i>
          <%# <h3>Tercer Paso</h3> %>
          <h4 class="custom-text"> Recibe solicitudes de Partners</h4>
          
        </a>
      </div>
    </div>
  </section>
  <hr>

  <!--Buscar entrenamiento-->
  <div class="cards-banner-two text-center">
    <h2>¿No sabes cuál elegir?</h2>
    <h3>¡No importa! ¡nosotros te ayudamos!</h3>
      <div class="col-sm-7 mx-5 p-5 px-5 text-center">
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
        <%= image_tag "logo.png", class: "m-3 p-3", alt: "", width: "150", height: "150" %>
      </div>
    <a href="#"><%= link_to 'Explora', trainings_path %> </a>
  </div>
  <hr>

    <!--Random user API-->
    
    <section class="random">
      <h2 class="title text-left"> Ellos ya están entrenando!!</h2>
        <div class="container">
          <div class="row">            
            
            <!--User 1-->
          
                  <script>
                    fetch('https://randomuser.me/api/?results=1&inc=gender,name,nat=es')
                    .then((response) => response.json())
                    .then(function(data){
                        const users = data.results.map(function(user){
                            const node = document.createElement('div')
                            node.className = 'col'
                            node.innerHTML = ` 
                                  <div class="col p-3 md-4 user-profile">
                                  <img id="avatar" src="https://randomuser.me/api/portraits/med/men/5.jpg" />
                                  <p id="fullname">${user.name.first} ${user.name.last}</p>
                                </div>`
                                
                            
                            return node
                        })
                        const section = document.querySelector('.random')
                        users.forEach((user) => section.appendChild(user))
                    })
                    .catch((error) => console.error(error))
                </script>
              
          

            <!--User 2-->
            
                <script>
                  fetch('https://randomuser.me/api/?results=1&inc=gender,name,nat=es')
                  .then((response) => response.json())
                  .then(function(data){
                      const users = data.results.map(function(user){
                          const node = document.createElement('div')
                          node.className = 'col'
                          node.innerHTML = ` 
                                <div class="col p-3 md-4 user-profile">
                                  <img id="avatar" src="https://randomuser.me/api/portraits/med/men/8.jpg" />
                                  <p id="fullname">${user.name.first} ${user.name.last}</p>
                                </div>`
                                
                          return node
                      })
                      const section = document.querySelector('.random')
                      users.forEach((user) => section.appendChild(user))
                  })
                  .catch((error) => console.error(error))
                </script>
              

            <!--User 3-->
                <script>
                  fetch('https://randomuser.me/api/?results=1&inc=gender,name,nat=es')
                  .then((response) => response.json())
                  .then(function(data){
                      const users = data.results.map(function(user){
                          const node = document.createElement('div')
                          node.className = 'col'
                          node.innerHTML = ` 
                                <div class="col p-3 md-4 user-profile">
                                  <img id="avatar" src="https://randomuser.me/api/portraits/med/women/10.jpg" />
                                  <p id="fullname">${user.name.first} ${user.name.last}</p>
                                </div>`
                                
                          return node
                      })
                      const section = document.querySelector('.random')
                      users.forEach((user) => section.appendChild(user))
                  })
                  .catch((error) => console.error(error))
                </script>
          </div>
        </div>
    </section>

    <!--Carrousel-->      
    <section>
      <%= render 'shared/carousel' %>
    </section>

  </section>
</div>